Currently finds Drive Test appointments only.

Run with 'ruby appointment.rb < input.txt > output.html' and
refresh output.html in a browser window every few seconds.

Edit the credentials first. You're ready to go!